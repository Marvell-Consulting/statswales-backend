/* eslint-disable no-shadow */
/* eslint-disable no-unused-vars */
export enum AppEnv {
    LOCAL = 'local',
    CI = 'ci',
    STAGING = 'staging',
    PROD = 'production',
    DEFAULT = 'default'
}

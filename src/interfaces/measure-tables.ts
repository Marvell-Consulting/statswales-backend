export interface MeasureTable {
  reference: string;
  description: string;
  notes: string;
  sort_order: string;
  format: string;
  decimals: number;
  measure_type: string;
  hierarchy: string;
}

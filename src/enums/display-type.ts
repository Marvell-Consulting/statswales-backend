export enum DisplayType {
  Boolean = 'boolean',
  Decimal = 'decimal',
  Float = 'float',
  Integer = 'integer',
  Long = 'long',
  Percentage = 'percentage',
  String = 'string',
  Text = 'text',
  Date = 'date',
  DateTime = 'datetime',
  Time = 'time',
  Timestamp = 'timestamp'
}
